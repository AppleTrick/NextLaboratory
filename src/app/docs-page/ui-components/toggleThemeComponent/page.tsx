import ToggleThemeComponent from "./components/ToggleThemeComponent";

const page = () => {
  return (
    <div>
      <div>토글 컴포넌트 입니다.</div>
      <ToggleThemeComponent />
    </div>
  );
};

export default page;
